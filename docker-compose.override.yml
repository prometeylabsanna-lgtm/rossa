services:
  backend:
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - .env
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.develop
      PYTHONPATH: /app/src

  db:
    env_file:
      - .env
    ports:
      - "5432:5432"
